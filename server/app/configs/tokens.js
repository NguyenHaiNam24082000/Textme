const tokenTypes = {
  //the following tokens are used in the server
  ACCESS: "access",
  REFRESH: "refresh",
  RESET_PASSWORD: "reset_password",
  VERIFY_EMAIL: "verify_email",
};

module.exports = { tokenTypes };
